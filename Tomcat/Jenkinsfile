pipeline {
    agent any

    tools {
      maven 'localMaven'
      git 'localGit'
    }

    triggers {
         pollSCM('* * * * *')
     }

stages{
        stage('Maven Build'){
            steps {
              sh 'mvn clean package'
            }
        }
        stage('Build'){
          parallel {
            stage ('Building Development') {
              environment {
                ENV = 'dev'
              }
              steps {
                  sh 'tree'
                  echo "${CHANGE_ID}"
                  sh 'docker build . -t dkheyman/trial-repo:${CHANGE_ID} -t dkheyman/trial-repo:${env.ENV}}'
                  sh 'docker push dkheyman/trial-repo:${env.CHANGE_ID}'
              }
              post {
                  success {
                      echo 'Successfully built docker dev image'
                  }
              }
            }
            stage ('Building Production') {
              environment {
                ENV = 'prod'
              }
              steps {
                  sh 'export GIT_TAG=`git log -1 --pretty=%h`'
                  echo  "${GIT_TAG}"
                  sh 'docker build . -t dkheyman/trial-repo:${env.CHANGE_ID} -t dkheyman/trial-repo:${env.ENV}'
                  sh 'docker push dkheyman/trial-repo:${env.CHANGE_ID}'
              }
              post {
                  success {
                      echo 'Successfully built docker prod image'
                  }
              }
            }
          }
        }

        stage ('Deployments'){
            parallel{
                stage ('Deploy to Development'){
                    steps {
                        sh "export BUILD_ENV=dev"
                        sh "cat init.sh | ssh ec2-user@20.0.3.28 -i /var/lib/jenkins/.ssh/ec2 'bash -ex' "
                    }
                }

                stage ("Deploy to Production"){
                    steps {
                        sh "export BUILD_ENV=prod"
                        sh "cat init.sh | ssh ec2-user@20.0.3.15 -i /var/lib/jenkins/.ssh/ec2 'bash -ex' "
                    }
                }
            }
        }
    }
}
